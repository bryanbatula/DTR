{
  "name": "dtr-backend",
  "version": "1.0.0",
  "description": "Daily Time Record System — Philippine Labor Code Compliant API",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/config/migrate.js && node src/app.js",
    "dev": "nodemon --signal SIGTERM src/app.js",
    "migrate": "node src/config/migrate.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
