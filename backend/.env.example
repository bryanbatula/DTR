# ── Local Development ─────────────────────────────────────────
PORT=3000
DB_USER=postgres
DB_HOST=localhost
DB_NAME=dtr
DB_PASSWORD=1234
DB_PORT=5432

# ── Production (Render.com — overrides individual params above) ─
# DATABASE_URL=postgresql://user:password@host:5432/dtr_db
